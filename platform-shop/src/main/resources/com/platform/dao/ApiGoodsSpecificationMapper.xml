<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.ApiGoodsSpecificationMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.platform.vo.GoodsSpecificationVo" id="goodsSpecificationMap">
        <result property="id" column="id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="goods_id" column="goods_id"/>
        <result property="specification_id" column="specification_id"/>
        <result property="value" column="value"/>
        <result property="pic_url" column="pic_url"/>
        <result property="name" column="name"/>
    </resultMap>

    <select id="queryObject" resultMap="goodsSpecificationMap">
		select * from goods_specification where id = #{value}
	</select>

    <select id="queryList" resultMap="goodsSpecificationMap">
        select
        <if test="fields != null and fields != ''">
            ${fields}
        </if>
        <if test="fields == null or fields == ''">
            gs.*
        </if>
        from goods_specification gs
        <if test="specification == true">
            inner join specification s on gs.specification_id= s.id
        </if>
        <where>
            <if test="merchantId!=null">
                and gs.merchant_id=#{merchantId}
            </if>
            <if test="goods_id != null">
                and gs.goods_id = #{goods_id}
            </if>
            <if test="ids != null">
                and gs.id in
                <foreach item="id" collection="ids" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
        </where>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by gs.id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="queryTotal" resultType="int">
        select count(*) from goods_specification
        WHERE 1=1
        <if test="merchantId!=null">
            and merchant_id=#{merchantId}
        </if>
    </select>

    <insert id="save" parameterType="com.platform.vo.GoodsSpecificationVo" useGeneratedKeys="true" keyProperty="id">
		insert into goods_specification
		(
		    `merchant_id`,
			`goods_id`, 
			`specification_id`, 
			`value`, 
			`pic_url`
		)
		values
		(
		    #{merchantId},
			#{goods_id}, 
			#{specification_id},
			#{value}, 
			#{pic_url}
		)
	</insert>

    <update id="update" parameterType="com.platform.vo.GoodsSpecificationVo">
        update goods_specification
        <set>
            <if test="goods_id != null">`goods_id` = #{goods_id},</if>
            <if test="specification_id != null">`specification_id` = #{specification_id},</if>
            <if test="value != null">`value` = #{value},</if>
            <if test="pic_url != null">`pic_url` = #{pic_url}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
		delete from goods_specification where id = #{value}
	</delete>

    <delete id="deleteBatch">
        delete from goods_specification where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>