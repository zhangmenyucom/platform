<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.ApiUserCouponMapper">

    <!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.platform.vo.UserCouponVo" id="userCouponMap">
        <result property="id" column="id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="coupon_id" column="coupon_id"/>
        <result property="coupon_number" column="coupon_number"/>
        <result property="user_id" column="user_id"/>
        <result property="used_time" column="used_time"/>
        <result property="add_time" column="add_time"/>
        <result property="orderSn" column="order_sn"/>
        <result property="source_key" column="source_key"/>
        <result property="referrer" column="referrer"/>
    </resultMap>

    <select id="queryObject" resultMap="userCouponMap">
		select * from user_coupon where id = #{value}
	</select>

    <select id="queryByCouponNumber" resultMap="userCouponMap">
        select * from user_coupon
        WHERE 1=1
        <if test="merchantId!=null">
            and merchant_id=#{merchantId}
        </if>
        and coupon_number = #{coupon_number}
    </select>

    <select id="queryList" resultMap="userCouponMap">
        select a.* from user_coupon a
        left join coupon b on b.id = a.coupon_id
        where 1 = 1
        <if test="merchantId!=null">
            and a.merchant_id=#{merchantId}
        </if>
        <if test="coupon_number != null">
            and a.coupon_number = #{coupon_number}
        </if>
        <if test="user_id != null">
            and a.user_id = #{user_id}
        </if>
        <if test="send_type != null">
            and b.send_type = #{send_type}
        </if>
        <if test="referrer != null">
            and a.referrer = #{referrer}
        </if>
        <if test="source_key != null">
            and a.source_key = #{source_key}
        </if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="queryTotal" resultType="int">
		select count(*) from user_coupon
        WHERE 1=1
        <if test="merchantId!=null">
            and merchant_id=#{merchantId}
        </if>
	</select>

    <insert id="save" parameterType="com.platform.vo.UserCouponVo" useGeneratedKeys="true" keyProperty="id">
		insert into user_coupon
		(
			`merchant_id`,
			`coupon_id`, 
			`coupon_number`, 
			`user_id`, 
			`used_time`, 
			`order_sn`,
			`add_time`,
			`source_key`,
			`referrer`
		)
		values
		(
		    #{merchantId},
            #{coupon_id},
			#{coupon_number},
			#{user_id},
			#{used_time},
			#{orderSn},
			#{add_time},
			#{source_key},
			#{referrer}
		)
	</insert>

    <update id="update" parameterType="com.platform.vo.UserCouponVo">
        update user_coupon
        <set>
            <if test="coupon_id != null">`coupon_id` = #{coupon_id},</if>
            <if test="coupon_number != null">`coupon_number` = #{coupon_number},</if>
            <if test="user_id != null">`user_id` = #{user_id},</if>
            <if test="used_time != null">`used_time` = #{used_time},</if>
            <if test="order_sn != null">`order_sn` = #{orderSn},</if>
            <if test="add_time != null">`add_time` = #{add_time},</if>
            <if test="source_key != null">`source_key` = #{source_key},</if>
            <if test="referrer != null">`referrer` = #{referrer}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
		delete from user_coupon where id = #{value}
	</delete>

    <delete id="deleteBatch">
        delete from user_coupon where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>