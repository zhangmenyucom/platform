<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.SysUserTagDao">

    <resultMap type="com.platform.entity.SysUserTagEntity" id="sysUserTagMap">
        <result property="id" column="id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="tag" column="tag"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="queryObject" resultType="com.platform.entity.SysUserTagEntity">
        select
        `id`,
        `merchant_id`,
        `tag`,
        `create_time`,
        `update_time`
        from sys_user_tag
        where id = #{id}
    </select>

    <select id="queryList" resultType="com.platform.entity.SysUserTagEntity">
        select
        `id`,
        `merchant_id`,
        `tag`,
        `create_time`,
        `update_time`
        from sys_user_tag
        WHERE 1=1
        <if test="merchantId!=null">
            and merchant_id=#{merchantId}
        </if>
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="queryTotal" resultType="int">
        select count(*) from sys_user_tag
        WHERE 1=1
        <if test="merchantId!=null">
            and merchant_id=#{merchantId}
        </if>
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
    </select>

    <insert id="save" parameterType="com.platform.entity.SysUserTagEntity" useGeneratedKeys="true" keyProperty="id">
        insert into sys_user_tag(
        `merchant_id`,
        `tag`,
        `create_time`,
        `update_time`)
        values(
        #{merchantId},
        #{tag},
        now(),
        now())
    </insert>

    <update id="update" parameterType="com.platform.entity.SysUserTagEntity">
        update sys_user_tag
        <set>
            <if test="merchantId != null">`merchant_id` = #{merchantId},</if>
            <if test="tag != null">`tag` = #{tag},</if>
            <if test="createTime != null">`create_time` = #{createTime},</if>
            <if test="updateTime != null">`update_time` = #{updateTime}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
        delete from sys_user_tag where id = #{value}
    </delete>

    <delete id="deleteBatch">
        delete from sys_user_tag where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>