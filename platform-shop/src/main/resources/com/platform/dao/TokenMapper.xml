<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.ApiTokenMapper">

	<select id="queryByUserId" resultType="com.platform.vo.TokenEntity">
		select * from tb_token where user_id = #{value}
	</select>

	<select id="queryByToken" resultType="com.platform.vo.TokenEntity">
		select * from tb_token
		where  token = #{value}
	</select>
	 
	<insert id="save" parameterType="com.platform.vo.TokenEntity">
		insert into tb_token
		(
			`merchant_id`,
			`user_id`, 
			`token`, 
			`expire_time`, 
			`update_time`
		)
		values
		(
		    #{merchantId},
			#{userId}, 
			#{token}, 
			#{expireTime}, 
			#{updateTime}
		)
	</insert>
	 
	<update id="update" parameterType="com.platform.vo.TokenEntity">
		update tb_token 
		<set>
			<if test="token != null">`token` = #{token}, </if>
			<if test="expireTime != null">`expire_time` = #{expireTime}, </if>
			<if test="updateTime != null">`update_time` = #{updateTime}</if>
		</set>
		where user_id = #{userId}
	</update>

</mapper>