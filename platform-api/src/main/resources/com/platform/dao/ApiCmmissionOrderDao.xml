<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.ApiCmmissionOrderDao">

    <resultMap type="com.platform.entity.CmmissionOrderVo" id="cmmissionOrderMap">
        <result property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="gainBalance" column="gain_balance"/>
        <result property="sourceUserId" column="source_user_id"/>
        <result property="detail" column="detail"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

	<select id="queryObject" resultType="com.platform.entity.CmmissionOrderVo">
		select
			`id`,
			`order_id`,
			`gain_balance`,
			`source_user_id`,
			`detail`,
			`create_time`,
			`update_time`
		from cmmission_order
		where id = #{id}
	</select>

	<select id="queryList" resultType="com.platform.entity.CmmissionOrderVo">
		select
    		`id`,
    		`order_id`,
    		`gain_balance`,
    		`source_user_id`,
    		`detail`,
    		`create_time`,
    		`update_time`
		from cmmission_order
		WHERE 1=1
		<if test="name != null and name.trim() != ''">
			AND name LIKE concat('%',#{name},'%')
		</if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
			<otherwise>
                order by id desc
			</otherwise>
        </choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
 	<select id="queryTotal" resultType="int">
		select count(*) from cmmission_order
		WHERE 1=1
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
	</select>
	 
	<insert id="save" parameterType="com.platform.entity.CmmissionOrderVo" useGeneratedKeys="true" keyProperty="id">
		insert into cmmission_order(
			`order_id`,
			`gain_balance`,
			`source_user_id`,
			`detail`,
			`create_time`,
			`update_time`)
		values(
			#{orderId},
			#{gainBalance},
			#{sourceUserId},
			#{detail},
			#{createTime},
			#{updateTime})
	</insert>
	 
	<update id="update" parameterType="com.platform.entity.CmmissionOrderVo">
		update cmmission_order 
		<set>
			<if test="orderId != null">`order_id` = #{orderId}, </if>
			<if test="gainBalance != null">`gain_balance` = #{gainBalance}, </if>
			<if test="sourceUserId != null">`source_user_id` = #{sourceUserId}, </if>
			<if test="detail != null">`detail` = #{detail}, </if>
			<if test="createTime != null">`create_time` = #{createTime}, </if>
			<if test="updateTime != null">`update_time` = #{updateTime}</if>
		</set>
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from cmmission_order where id = #{value}
	</delete>
	
	<delete id="deleteBatch">
		delete from cmmission_order where id in 
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>

</mapper>