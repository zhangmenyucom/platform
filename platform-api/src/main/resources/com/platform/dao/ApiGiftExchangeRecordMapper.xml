<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.ApiGiftExchangeRecordMapper">

    <resultMap type="com.platform.entity.GiftExchangeRecordEntityVo" id="giftExchangeRecordMap">
        <result property="id" column="id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="userId" column="user_id"/>
        <result property="giftId" column="gift_id"/>
        <result property="usePoint" column="use_point"/>
        <result property="orderSn" column="order_sn"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="queryObject" resultType="com.platform.entity.GiftExchangeRecordEntityVo">
		select
			`id`,
            `merchant_id`,
			`user_id`,
			`gift_id`,
			`use_point`,
			`order_sn`,
			`create_time`,
			`update_time`
		from gift_exchange_record
		where id = #{id}
	</select>

    <select id="queryList" resultType="com.platform.entity.GiftExchangeRecordEntityVo">
        select
        r.`id`,
        r.`user_id`,
        r.`merchant_id`,
        r.`user_id`,
        u.nickname as nickName,
        g.name as giftName,
        r.`gift_id`,
        r.`use_point`,
        r.`order_sn`,
        r.`create_time`,
        r.`update_time`
        from gift_exchange_record r
        left join gift g on r.gift_id=g.id
        left join user u on u.id=r.user_id
        WHERE 1=1
        <if test="merchantId!=null">
            and r.merchant_id=#{merchantId}
        </if>
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
        <if test="userId != null">
            AND r.user_id=#{userId}
        </if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="queryTotal" resultType="int">
        select
        count(1)
        from gift_exchange_record r
        where 1=1
        <if test="merchantId!=null">
            and merchant_id=#{merchantId}
        </if>
        <if test="userId != null">
            AND r.user_id=#{userId}
        </if>
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
    </select>

    <insert id="save" parameterType="com.platform.entity.GiftExchangeRecordEntityVo" useGeneratedKeys="true" keyProperty="id">
		insert into gift_exchange_record(
		    `merchant_id`,
			`user_id`,
			`gift_id`,
			`use_point`,
			`order_sn`,
			`create_time`,
			`update_time`)
		values(
		    #{merchantId},
			#{userId},
			#{giftId},
			#{usePoint},
			#{orderSn},
			#{createTime},
			#{updateTime})
	</insert>

    <update id="update" parameterType="com.platform.entity.GiftExchangeRecordEntityVo">
        update gift_exchange_record
        <set>
            <if test="userId != null">`user_id` = #{userId},</if>
            <if test="giftId != null">`gift_id` = #{giftId},</if>
            <if test="usePoint != null">`use_point` = #{usePoint},</if>
            <if test="orderSn != null">`order_sn` = #{orderSn},</if>
            <if test="createTime != null">`create_time` = #{createTime},</if>
            <if test="updateTime != null">`update_time` = #{updateTime}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
		delete from gift_exchange_record where id = #{value}
	</delete>

    <delete id="deleteBatch">
        delete from gift_exchange_record where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>