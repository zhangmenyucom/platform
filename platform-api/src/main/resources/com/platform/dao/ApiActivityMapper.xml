<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.dao.ApiActivityMapper">

    <resultMap type="com.platform.entity.ActivityVo" id="activityMap">
        <result property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="author" column="author"/>
        <result property="content" column="content"/>
        <result property="banner" column="banner"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="position" column="position"/>
    </resultMap>

    <select id="queryObject" resultType="com.platform.entity.ActivityVo">
		select
			`id`,
			`title`,
			`author`,
			`content`,
			`banner`,
			`start_date`,
			`end_date`,
			`position`
		from activity
		where id = #{id}
	</select>

    <select id="queryList" resultType="com.platform.entity.ActivityVo">
        select
        `id`,
        `title`,
        `author`,
        `content`,
        `banner`,
        `start_date`,
        `end_date`,
        `position`
        from activity
        WHERE 1=1
        <if test="merchantId!=null">
            and merchant_id=#{merchantId}
        </if>
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="queryTotal" resultType="int">
        select count(*) from activity
        WHERE 1=1
        <if test="merchantId!=null">
            and merchant_id=#{merchantId}
        </if>
        <if test="name != null and name.trim() != ''">
            AND name LIKE concat('%',#{name},'%')
        </if>
    </select>

    <insert id="save" parameterType="com.platform.entity.ActivityVo">
		insert into activity(
			`id`,
			`title`,
			`author`,
			`content`,
			`banner`,
			`start_date`,
			`end_date`,
			`position`)
		values(
			#{id},
			#{title},
			#{author},
			#{content},
			#{banner},
			#{startDate},
			#{endDate},
			#{position})
	</insert>

    <update id="update" parameterType="com.platform.entity.ActivityVo">
        update activity
        <set>
            <if test="title != null">`title` = #{title},</if>
            <if test="author != null">`author` = #{author},</if>
            <if test="content != null">`content` = #{content},</if>
            <if test="banner != null">`banner` = #{banner},</if>
            <if test="startDate != null">`start_date` = #{startDate},</if>
            <if test="endDate != null">`end_date` = #{endDate},</if>
            <if test="position != null">`position` = #{position}</if>
        </set>
        where id = #{id}
    </update>

    <delete id="delete">
		delete from activity where id = #{value}
	</delete>

    <delete id="deleteBatch">
        delete from activity where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>